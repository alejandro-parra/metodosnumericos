    
    def minMaxLengthAverage(lst):
	mini = min(lst);
	maxi = max(lst);
	lengt = len(lst);
	mid = (sum(lst))/c
	lista = [];
	lista.append(mini);
	lista.append(maxi);
	lista.append(lengt);
	lista.append(mid);
	return lista;
  

	
